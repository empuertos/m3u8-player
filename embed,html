<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Embedded Fixed-size Video.js iframe</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    body {
      margin:0;
      background:#111;
      color:#eee;
      font-family: system-ui,-apple-system,BlinkMacSystemFont,sans-serif;
      display:flex;
      flex-direction:column;
      align-items:center;
      padding:1rem;
      gap:1rem;
    }
    h1 { margin:0; font-size:1.5rem; }
    .iframe-wrapper {
      width:640px;
      height:360px;
      border:2px solid #444;
      border-radius:8px;
      overflow:hidden;
      background:#000;
      flex: none;
    }
    iframe {
      width:640px;
      height:360px;
      border:0;
      display:block;
    }
    .note {
      font-size:0.9rem;
      opacity:.8;
      max-width:640px;
      text-align:center;
    }
    @media (max-width: 700px) {
      /* allow scrolling if viewport narrower than the fixed iframe */
      .container {
        overflow-x: auto;
      }
    }
  </style>
</head>
<body>
  <h1>Fixed-size Video.js Player in iframe</h1>
  <div class="container">
    <div class="iframe-wrapper">
      <iframe
        src="player.html"
        title="Video.js fixed HLS player"
        loading="lazy"
        allow="autoplay; fullscreen; picture-in-picture"
        allowfullscreen
        referrerpolicy="strict-origin-when-cross-origin"
      ></iframe>
    </div>
  </div>
  <div class="note">
    The video is constrained to 640×360px on both mobile and desktop; smaller viewports will scroll instead of shrinking the player. Adjust the iframe `src` to the hosted path if not same directory.
  </div>
</body>
</html>
